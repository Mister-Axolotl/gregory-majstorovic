; Active la sonde de sante native de php-fpm.
; A copier dans /usr/local/etc/php-fpm.d/ depuis votre Dockerfile.
; Le HEALTHCHECK l'interroge ensuite avec cgi-fcgi (paquet `fcgi`).
[www]
ping.path = /ping
ping.response = pong
